import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    nextQuestion: {
        marginTop: 16,
        color: '#fff'
    },
    nextQuestionText: {
        color: '#fff',
        fontSize: 18
    },
    nextQuestionIcon: {
        fontSize: 18
    }
})